{
  "name": "@agentx-fast/cli",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/arpayidcloud-byte/Agentx.git",
    "directory": "apps/cli"
  },
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "agentx": "./bin/agentx"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "typescript": "5.4.5",
    "vitest": "^3.2.6"
  },
  "dependencies": {
    "@agentx-fast/agent-platform": "workspace:*",
    "@agentx-fast/core-runtime": "workspace:*",
    "@agentx-fast/provider-sdk": "workspace:*",
    "@agentx-fast/runtime-adapters": "workspace:*",
    "@agentx-fast/runtime-production": "workspace:*",
    "@agentx-fast/tool-sdk": "workspace:*",
    "@prisma/client": "^7.9.0",
    "commander": "^15.0.0"
  }
}
